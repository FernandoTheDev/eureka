use "io.ek" : { print, println }

extern func str[] array_push(...);

let names str[] = ["Fernando" "Tyler Laceby" ["TheDevConnor"]] // safe
names += cast!str(100)
println(names)

// println(names)

let ages int[] = [17, 99, 69] // safe
println(ages)

let unsafe mixed[] = ["Fernando", 1, 2.1421f] // unsafe array
println(unsafe)

// names += cast!str(100) // cast explicito = segurança
// other += 10 // sem verificação pois é mixed

// builtin
// names.pop()

// let names_len int = names.length()
